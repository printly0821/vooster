{
  "name": "@vooster/extension",
  "version": "0.1.0",
  "description": "Vooster Display Launcher - Chrome Extension for Remote Display Control",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "preact": "^10.19.3",
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.258",
    "@types/qrcode": "^1.5.5",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  },
  "private": true
}
