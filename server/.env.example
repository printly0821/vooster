# Socket.IO 서버 환경변수 템플릿
# 이 파일을 복사하여 server/.env로 저장하고 값을 설정하세요
# cp server/.env.example server/.env

# 서버 설정
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# CORS 허용 원본 (쉼표로 구분)
# 개발: localhost + Chrome Extension 허용
# 프로덕션: 실제 도메인으로 변경
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,chrome-extension://*

# JWT 비밀키 (최소 32자 권장)
# ⚠️  프로덕션에서 반드시 강력한 시크릿으로 변경하세요!
SOCKET_JWT_SECRET=your-secret-key-min-32-chars-change-in-production

# WebSocket URL (클라이언트에서 사용)
# 개발: ws://localhost:3001
# 프로덕션: wss://your-domain.com
WS_URL=ws://localhost:3001

# 페어링 URL 베이스
# QR 코드에 포함될 URL
PAIRING_URL_BASE=http://localhost:3000/pair

# Redis URL (선택, Socket.IO Redis Adapter 사용 시)
# Docker Compose: redis://redis:6379
# 로컬: redis://localhost:6379
# REDIS_URL=redis://localhost:6379
